<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <record id="view_analytic_account_form" model="ir.ui.view">
        <field name="name">account.analytic.account.form</field>
        <field name="model">account.analytic.account</field>
        <field name="inherit_id" ref="account.view_account_analytic_account_form"/>
        <field name="arch" type="xml">
            <group name="group_account_information" position="after">
                <field name="is_blocked"/>
            </group>
        </field>
    </record>

    <!-- Modificamos la vista de selección de cuentas analíticas -->
    <record id="view_analytic_account_search" model="ir.ui.view">
        <field name="name">account.analytic.account.search</field>
        <field name="model">account.analytic.account</field>
        <field name="inherit_id" ref="account.view_account_analytic_account_filter"/>
        <field name="arch" type="xml">
            <field name="analytic_account_id" position="attributes">
                <attribute name="domain">[('is_blocked', '=', False)]</attribute>
            </field>
        </field>
    </record>
</odoo>
